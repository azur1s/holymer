let fizz = fun (n) -> if n % 3 == 0 && n % 5 == 0
    then "FizzBuzz"
    else if n % 3 == 0
    then "Fizz"
    else if n % 5 == 0
    then "Buzz"
    else ""
    ;

let println = fun (a) -> ();
println(fizz(5));