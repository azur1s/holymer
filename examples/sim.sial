fun foo x = do
    x
end

fun fac n = if n == 0 then 1 else n * fac(n - 1)

fun main = do
    let succ = \x -> x + 1,
        n = 34,
    in
        println(n + succ(n))

    print("Hello ")
    println("World!")

    println(foo(1))
    println(fac(5))
end